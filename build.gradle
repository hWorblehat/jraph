plugins {
	id 'java-library'
	id 'groovy'
	id 'jacoco'
}

sourceCompatibility = 1.8
targetCompatibility = 1.8

apply from: 'dependencies.gradle'

if(System.env.CI) {
	jacocoTestReport {
		reports {
			xml.enabled true
			html.enabled false
		}
	}
}
